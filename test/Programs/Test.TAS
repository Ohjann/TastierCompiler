// This is a test program which can be compiled by the TastierCompiler.
// It reads a sequence of numbers from the input file and computes, for each
// number, the sum of all integers up to that number

program Test {


  struct myStruct {
      int i;
      int j;
      bool yea;
  };

  struct anotherOne {
      int i;
      int j;
  };
  const k :=3;
  
  void Main() {
      struct myStruct a;
      struct myStruct hello; 
      struct anotherOne pleasework;
      hello.i := 44;
      a.i := 22;
      pleasework.i := 999;
      write a.i;
      write hello.i;
      write pleasework.i;
  }
}
